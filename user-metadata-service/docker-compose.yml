version: '3'

services:

  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: userdb
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"

  user-service:
    build: .
    ports:
      - "8080:8080"
    depends_on:
      - postgres